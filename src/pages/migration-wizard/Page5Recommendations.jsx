/**
 * Page 5: Review & Export — renders the same full ExportPage component
 * (which already has the MigrationSidebar wizard layout, step 5 active,
 * package contents, download buttons, AI summary, next steps, etc.)
 */
import ExportPage from '../ExportPage.jsx';

export default function Page5Recommendations() {
  return <ExportPage />;
}
